---
- name: copy file from Controller to Remote Host
  hosts: planetexpress

  tasks:
          - name: Copy file to remote hosts
            copy:
                    src: ~/ans/files/firewall.settings
                    dest: ~/
